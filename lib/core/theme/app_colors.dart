import 'package:flutter/material.dart';

class AppColors {
  static const Color matteBlack = Color(0xFF0B0B0D);
  static const Color darkGray = Color(0xFF1A1A1D);
  static const Color softWhite = Color(0xFFF5F5F5);
  static const Color luxuryGold = Color(0xFFD4AF37);
  static const Color successGreen = Color(0xFF2ECC71);
  static const Color dangerRed = Color(0xFFE74C3C); // Added based on typical needs, though not explicitly in prompt list but good practice.
  
  // Additional shades for gradients/UI depth if needed
  static const Color goldHighlight = Color(0xFFFFD700);
}
